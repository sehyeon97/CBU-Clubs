import 'package:flutter/material.dart';

class ScrollableButton extends StatelessWidget {
  const ScrollableButton({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
